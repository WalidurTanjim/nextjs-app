const SectionTitle = ({ title }) => {
    return (
        <div className="section-title mb-7 text-center">
            <h1 className="text-2xl font-medium text-slate-800">{title}</h1>
        </div>
    );
};

export default SectionTitle;